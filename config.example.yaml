# Flux Workflow Engine — MCP Server 配置示例
# 复制为 config.yaml 并根据需要修改

# DeepSeek API（DAGPlanner 生成 DAG 用）
llm:
  api_key: "sk-your-deepseek-api-key"
  base_url: "https://api.deepseek.com/v1"
  model: "deepseek-v4-pro"
